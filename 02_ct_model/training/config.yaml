# CT Model Training Configuration

data:
  batch_size: 2
  num_workers: 0

model:
  num_classes: 2

training:
  epochs: 20
  learning_rate: 0.0001
  weight_decay: 0.0001

  patience: 5

  checkpoint_dir: "02_ct_model/checkpoints"

seed: 42